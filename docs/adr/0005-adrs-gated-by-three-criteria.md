# ADRs are gated by the three criteria, enforced in the app

A proposed ADR must justify against all three criteria — hard to reverse, surprising without context, the result of a real trade-off — or it becomes a CONTEXT.md note or nothing. Documentation-by-default is what poisons most ADR directories; the gate keeps the decision log high-signal.

## Consequences

Requires judgment at the gate.
